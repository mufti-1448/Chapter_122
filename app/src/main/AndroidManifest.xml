<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Chapter11"
        tools:targetApi="31">
        <activity
            android:name=".database.ListTemanActivity"
            android:exported="false" />
        <activity
            android:name=".database.DetailTemanActivity"
            android:exported="false" />
        <activity
            android:name=".database.AddFriendActivity"
            android:exported="true" />
        <activity
            android:name=".database.ListFriendActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SaveMyNameActivity"
            android:exported="true" />
    </application>

</manifest>